# ============================================================
# Azure Container Apps — Deployment Configuration
# Copy this file to .env.aca and fill in your values.
# NEVER commit .env.aca to Git (it contains secrets).
# ============================================================

# ── Azure Resources (customise or keep defaults) ──
RESOURCE_GROUP=rg-archive-classifier
LOCATION=uksouth
ACR_NAME=acrarchivedocs
ENV_NAME=aca-env-archive
APP_NAME=archive-doc-classifier
IMAGE_TAG=latest

# ── Databricks Connection (REQUIRED) ──
DATABRICKS_HOST=https://adb-2223129477745111.11.azuredatabricks.net
DATABRICKS_TOKEN=dapi...
TRAINING_JOB_ID=922802212201609

# ── MLflow (defaults usually fine) ──
MLFLOW_EXPERIMENT=/Users/hakeemfujah@tfl.gov.uk/experiments/ac_model_v2
